const userNameRegisterRegex = /^([A-Za-z0-9]){4,20}$/;
const decimalsRegex = /^\d+(\.\d{1,2})?$/;
const emailRegex = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;

export { userNameRegisterRegex, decimalsRegex, emailRegex };

// /^\d+\.\d{2,2}$/;
